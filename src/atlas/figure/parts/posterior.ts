import type { FillShape, StrokeShape } from "./types";

/**
 * Posterior flesh is the surface plate at /atlas/body-posterior.png.
 * It is a separate drawing, not a mirror of the anterior plate.
 */
export const POSTERIOR_FILLS: FillShape[] = [];
export const POSTERIOR_STROKES: StrokeShape[] = [];
export const POSTERIOR_CONTOURS: string[] = [];
